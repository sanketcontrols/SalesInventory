# Copy to .env in project root for Docker Compose on NAS
# cp .env.docker.example .env

# Synology DSM already uses port 5000 — use 5080
APP_PORT=5080
# Synology / host often uses 5432 — map DB to 5433 on the NAS
DB_HOST_PORT=5433

# Must match the password used the FIRST time the db volume was created.
# If you change this later, login will fail until you recreate the volume.
DB_NAME=billing
DB_USER=postgres
DB_PASSWORD=ChooseAStrongPassword123
JWT_SECRET=ChooseALongRandomSecretKeyForJWT

# Do NOT put DATABASE_URL (Render) in this file for NAS Docker.
# Compose forces DB_HOST=db and DB_SSL=false for the app container.
